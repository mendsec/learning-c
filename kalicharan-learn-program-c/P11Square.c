#include <studio.h>
int main() {
   int a, s;
   printf("Enter length of side: ");
   scanf("%d", &s); //store lengh in s
   a = s * s; //calculate are; store in a
   printf("\nArea of square is %d\n", a);
}
